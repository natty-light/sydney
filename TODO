add support for defining functions like:
func add(a, b) {
    a + b
}

add support integer division


add type checking at compile time


add builtins for casting 
QS3? remove null, follow Go's approach for zero value for uninit vars
