module "math"

pub func abs_int(int val) -> int {
    if (val < 0) { -val; } else { val };
}

pub func abs(float val) -> float {
    if (val < 0.0) { -val; } else { val; };
}

pub func min<T>(T a, T b) -> T {
    if (a < b) { a; } else { b; };
}

pub func max<T>(T a, T b) -> T {
    if (a > b) { a; } else { b; }
}